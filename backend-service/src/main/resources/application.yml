server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/ess_salary_advance}
    username: ${SPRING_DATASOURCE_USERNAME:ess_user}
    password: ${SPRING_DATASOURCE_PASSWORD:ess_password}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        "[format_sql]": true
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  jwt:
    secret: ${APP_JWT_SECRET:change-me-super-secure-jwt-secret-key-with-at-least-256-bits}
    expiration-ms: 3600000
  seed:
    password: ${APP_SEED_PASSWORD:T4actical#0067}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://172.104.131.185:3000}

logging:
  level:
    "[org.springframework.security]": INFO
    "[com.ess.salaryadvance]": DEBUG
    "[org.hibernate.SQL]": DEBUG
